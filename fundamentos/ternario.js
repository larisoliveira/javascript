// Abaixo utilizamos o ternário para nos retornar um valor. Basicamente, a estrutura abaixo é quase que um IF e ELSE
const resultado = nota => nota >= 7 ? 'Aprovado' : 'Reprovado'

console.log(resultado(5))
console.log(resultado(7.1))